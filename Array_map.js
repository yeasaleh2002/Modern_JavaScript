// map work look like for loop. it will check all the elements. and work every elements and result all the elements in a new array. 

var numbers = [1, 2, 3, 4, 5, 6]

let result = numbers.map((number) => { // it will return a new array
    console.log(number * 2)
})