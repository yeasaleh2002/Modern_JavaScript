// we can push element in the array. it will take multiple elements. it will change main array. 

let number = [1, 2, 3, 4];

var result = number.push([3, 34, 36]); 

console.log(number); 
console.log(result); // showing length